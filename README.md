# Water Quality Forecast App

This project predicts 30-day water quality parameters using a GRU + Attention model.

## Features
- Multivariate forecasting (pH, DO, temp, conductance, discharge)
- 30-day prediction
- Interactive Streamlit interface

## Run locally
pip install -r requirements.txt  
streamlit run app.py

## Deployment
Deployed via Streamlit Cloud